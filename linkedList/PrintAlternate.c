#include<stdio.h>
#include<stdlib.h>
// link list node 
struct Node {
    int data;
    struct Node * next;
};

//function to get the alternate nodes of the linked lists
void printAlternateNode(struct Node * head) {
    int count = 0; 
    while(head != NULL) {
       // when count is even print the nodes 
       if(count % 2 == 0) {
        
       }
    }

  }
